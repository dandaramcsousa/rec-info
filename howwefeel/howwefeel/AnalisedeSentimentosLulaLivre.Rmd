---
title: "#LulaLivre"
output: html_notebook
---



```{r}
knitr::opts_chunk$set(echo = TRUE, warning = FALSE, message = FALSE)
library(tidyverse)
library(ggplot2)
library(readr)
library(dplyr) 
theme_set(theme_minimal())
```


*Carregando os dados*
```{r}
datalulaframe <- read.csv("../lula.csv", encoding = "UTF-8")

dbpos <- read_delim("../dbpos.csv", 
    ";", escape_double = FALSE, trim_ws = TRUE)
dbneg <- read_delim("../dbneg.csv", 
    ";", escape_double = FALSE, trim_ws = TRUE) %>% filter(sentiment == 0)

db <- rbind(dbpos,dbneg)
db <- na.omit(db)


write.csv(dblula, file = "lula.csv")

```


```{r}

```

